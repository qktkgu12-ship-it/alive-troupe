# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (비밀 키가 들어가므로 절대 커밋 금지)
# .env*.local만 막으면 .env / .env.production은 그대로 올라간다 — 전부 막는다
.env
.env.*
# 예시 파일은 실제 키가 없으므로 저장소에 둔다
!.env.local.example

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# claude
.claude/

# 팜플렛 작업용 폴더(원본 PNG 백업·공유용 단일파일) — 배포본은 public/pamphlet/ 에 있음
/pamphlet/

# 로컬 백업 파일
CLAUDE.md.local.bak
*.local.bak
